const raio = 5.6;
//calculando a area de uma circunferencia, .pow eleva o numero a potencia de 2 no exemplo, .pi retorna o numero pi
const area = Math.PI * Math.pow(raio, 2);

console.log(area);
console.log(typeof Math);